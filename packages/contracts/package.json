{
  "name": "@tessera/contracts",
  "version": "0.1.0",
  "description": "Shared wire types and pure domain helpers for the Tessera API and web client",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "sideEffects": false,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "prepare": "pnpm run build"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
